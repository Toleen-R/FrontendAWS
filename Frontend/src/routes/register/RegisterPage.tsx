import { Register } from "@/components/register";  // Importerar Register-komponenten för att hantera användarregistrering.

export function RegisterPage() {
    <main className="flex justify-center items-center">  // Wrapper för att centrera innehållet på skärmen.
        <Register />  // Renderar Register-komponenten för användarregistrering.
    </main>;
}
